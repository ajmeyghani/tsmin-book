{
  "name": "md-converter",
  "version": "1.0.0",
  "description": "",
  "main": "convert.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf output && node bin/htx.js && node bin/highlight.js && node bin/pdfify.js",
    "start:hx": "rm -rf output && node bin/htx.js && node bin/pdfify.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^0.26.3"
  }
}
